# Installation

Option 1: Install from PyPI.

```bash
pip install pylint-nautobot
```

Option 2: Manually install via Poetry.

```bash
git clone https://github.com/nautobot/pylint-nautobot.git
cd pylint-nautobot
curl -sSL https://install.python-poetry.org | python3 -
poetry install
```

Option 3: Install from a GitHub branch, such as develop as shown below.

```bash
pip install git+https://github.com/nautobot/pylint-nautobot.git@develop
```
